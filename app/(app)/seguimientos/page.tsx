/**
 * Pantalla: Seguimientos — pantalla de entrada tras el login.
 * Atrasados (resaltados) + Para hoy, buscador, marcar como hecho.
 * Diseño: Design/…/Seguimientos.dc.html · Linear: KAR-22.
 */
export default function SeguimientosPage() {
  return (
    <section className="mx-auto max-w-2xl p-6">
      <h1 className="text-2xl font-bold text-text-primary">Seguimientos</h1>
      <p className="mt-2 text-base text-text-secondary">Pendiente de construir.</p>
    </section>
  );
}
