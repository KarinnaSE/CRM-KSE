# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (contienen secretos: NUNCA se suben a GitHub)
.env
.env.local
.env*.local

# convex: el código generado se VERSIONA a propósito. En el build de Railway,
# `next build` necesita convex/_generated/ y el checkout limpio no lo traería
# si estuviera ignorado. Se regenera con `npx convex codegen`.

# typescript
*.tsbuildinfo

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/

# scratch
/Tmp/

# herramientas locales / artefactos de depuración
/.playwright-mcp/
.claude/settings.local.json

# artefactos internos del proceso de planificación/auditoría (no se publican)
/plan.txt
/audit.txt
/codigo.txt
/audit_codigo.txt
